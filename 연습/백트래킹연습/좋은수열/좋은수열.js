function b2661() {
    var fs = require('fs');
    // var input = fs.readFileSync('/dev/stdin').toString();
    var n = fs.readFileSync(__dirname + '/stdin.txt').toString().replace(/\r/g, "");
    n = parseInt(n)
    // console.log(s)
}

function goodArray(){
    for(var i=1; i<=3; i++){
        if(isPromising()){
            
        }
    }
}

b2661()